# Handover

- #79 - Assumed: the executable EditorJS case preserves the original Slack thread as attached provenance while provider admission and thread continuation remain complementary coverage. Gap: stale repo-local `.workflow-data` hit `EMFILE`; all four strict behavior cases passed with an isolated `WORKFLOW_LOCAL_DATA_DIR`.
- #80 - Assumed: working-repository package scripts become trusted validator sources only after typed discovery and source-hash binding; documented checks add provenance but never become executable model input.
- #81 - Assumed: `expectedContentHash` hashes the complete raw bytes and `createOnly` asserts absence. Gap: repository-owned validator results remain read-only evidence instead of draft attachments. Live status: the focused existing-page case passed all 9 gates on exact microsandbox 0.5.7; local 0.5.10 instead wedged near 100% CPU after valid `authoring_workspace.apply` dispatch, while hosted execution separately exposed root-union arguments stringified before dispatch. Keep open for multi-file, correction, and failed-validation live proof through the supervisor.
- #82 - Assumed: context repositories default to source-code or merged-change evidence, maintainer-confirmed decisions require explicit configuration, and configured GitHub access fails closed. Live status: keep open until the discovered current-docs plus read-only-source eval executes through the supervisor.
- #83 - Assumed: sourced answers, abstentions, and gap recommendations remain ephemeral unless an explicit continuation creates docs work with ordinary provenance fields. Live status: keep open until the seven discovered answer, abstention, no-mutation, and continuation cases execute.
- #84 - Assumed: explicit manual work may use server-labelled external context while Slack and Linear identities remain exclusive to provider admission; evidence linking preserves lifecycle and uses `updatedAt` for optimistic concurrency. Live status: keep open until the migrated quick, substantial, correction, park/resume, follow-up, internal-document, and terminal-outcome cases execute.
- #85 - Assumed: approval resume belongs only to the original verified human initiator and exact approved Eve execution; schedule and watch principals cannot inherit it, and watch execution rechecks the exact reservation and grants before each call. Live status: deterministic matrix, replay, and compiled-manifest proof remain; the Slack, Linear, Eve, schedule, watch, and approval eval matrix still needs live execution.
- #86 - Assumed: live repository-scenario mappings remain documented until #88 removes their consumers; skills describe available procedures but cannot widen dynamic authority. Live status: identity, behavior-settings, routing, general-answer, and docs-work evals still need supervised execution.
- #60 - Assumed: raw handoff retention is a hard cap; provider delivery uses the exact verified Slack workspace and channel with server-owned routing, leases, budgets, batching, and idempotency; effective revisions without an exact `providerWorkspaceId` cannot receive watch authority. Live status: deterministic runtime, migration, adapter, and retry-race proof remain; live watch proof still needs execution.
- #77 - Assumed: continuity starts on the first exactly granted revision, uses that revision's finite retention, follows the stable watch across replacement revisions, and cannot be redirected by an active turn. Live status: the read, update, revise-in-place, and no-op eval still needs execution.
- #87 - Assumed: source revision and freshness stay unresolved until a verified read, capability pages project only the exact resolver output for a verified principal, and retained internal documents belong to work detail only while their current revision references that signal. Progress: shared source/capability projections and read-only pages are complete; work detail remains in progress because the draft groups artifacts by kind instead of exact current references, treats every signal link as evidence, silently caps linked internal documents, and lacks full-lifecycle assertions. Follow-up: authenticated watch mutation belongs to #61.
